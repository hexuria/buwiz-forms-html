# Explicit gaps

1. The post-submission `txtFinalFlag` and reopen transition is deliberately unobserved. The live 7.9.6 Submit / Final Copy path reached the explicit online-submission confirmation, which was canceled. Confirming it would attempt an online tax submission and is outside the research scope. The exact source path, direct virtualized `Encrypt.exe` execution, deterministic ciphertext, and byte-exact repository-algorithm decryption/preservation provide the available non-submission evidence.

Resolved on 2026-07-23: the live UI captured a representative first-error dialog and confirmed acceptance of birth month `00`, a blank spouse branch code, and blank spouse Item 25A, plus the no-spouse Edit-state enablement defect. See `fixtures/live-ui-observations-v1.json`.

